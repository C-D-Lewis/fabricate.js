import builder from './fabricate';

export {
  app,
  update,
  buildKey,
  declare,
  onKeyDown,
  conditional,
  clearState,
} from './fabricate';
export {
  isNarrow,
} from './utils';

export default builder;
